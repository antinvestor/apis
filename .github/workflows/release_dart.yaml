name: Release Dart Packages

on:
  push:
    tags:
      - "v*.*.*"
    paths:
      - 'dart/**'
      - 'proto/**'
  workflow_dispatch:
    inputs:
      force:
        description: 'Force release all packages'
        required: false
        type: boolean
        default: false

jobs:
  release:
    strategy:
      matrix:
        package: [common, chat, device, notification, ocr, partition, payment, profile, property, settings, ledger, lostid, files]
      fail-fast: false
    
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Get the latest tag
        id: get_tags
        run: |
          # Get the most recent tag that matches the module
          TAGS=$(git tag --sort=-creatordate | grep "^dart/${{ matrix.api_module }}/v" | head -n 1)
          echo "Found tag: $TAGS"
          if [[ -z "$TAGS" ]]; then
            echo "not_enough_tags=true" >> $GITHUB_OUTPUT
            echo "No tags found for dart/${{ matrix.api_module }}."
          else
            LATEST_TAG=$(echo "$TAGS" | head -n 1)
            SECOND_LATEST_TAG=$(echo "$TAGS" | sed -n '2p')
            echo "latest_tag=$LATEST_TAG" >> $GITHUB_ENV
            echo "second_latest_tag=$SECOND_LATEST_TAG" >> $GITHUB_ENV
            echo "not_enough_tags=false" >> $GITHUB_OUTPUT
          fi

      - name: Check for changes since the last tag
        if: steps.get_tags.outputs.not_enough_tags == 'false'
        id: dart_module_changed
        run: |
          CHANGES=$(git diff --name-only ${{ env.latest_tag }} HEAD | grep 'dart/${{ matrix.api_module }}' || true)
          if [[ -n "$CHANGES" ]]; then
            echo "Changes found in dart/${{ matrix.api_module }}"
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "No changes found in dart/${{ matrix.api_module }}"
            echo "changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Setup Dart
        if: steps.get_tags.outputs.not_enough_tags == 'true' || steps.dart_module_changed.outputs.changed == 'true'
        uses: dart-lang/setup-dart@v1
        with:
          sdk: stable
      
      - name: Configure Git
        if: steps.get_tags.outputs.not_enough_tags == 'true' || steps.dart_module_changed.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
      
      - name: Setup pub credentials
        if: steps.get_tags.outputs.not_enough_tags == 'true' || steps.dart_module_changed.outputs.changed == 'true'
        run: |
          mkdir -p ~/.config/dart
          echo '${{ secrets.PUB_DEV_CREDENTIALS }}' > ~/.config/dart/pub-credentials.json
      
      - name: Release package
        if: steps.get_tags.outputs.not_enough_tags == 'true' || steps.dart_module_changed.outputs.changed == 'true'
        run: |
          PACKAGE="${{ matrix.package }}"
          PACKAGE_DIR="dart/$PACKAGE"
          
          echo "================================================"
          echo "Processing package: $PACKAGE"
          echo "================================================"
          
          if [ ! -d "$PACKAGE_DIR" ]; then
            echo "  Package directory not found: $PACKAGE_DIR"
            exit 1
          fi
          
          cd "$PACKAGE_DIR"
          
          # Get package name
          PACKAGE_NAME=$(grep '^name:' pubspec.yaml | sed 's/name: //')
          echo "Package name: $PACKAGE_NAME"
          
          # Extract version from global tag (format: vX.Y.Z)
          TAG_NAME="${{ github.ref_name }}"
          if [[ "$TAG_NAME" =~ ^v([0-9]+\.[0-9]+\.[0-9]+)$ ]]; then
            NEW_VERSION="${BASH_REMATCH[1]}"
            echo "Release version from tag: $NEW_VERSION"
          else
            echo "❌ Could not parse version from tag: $TAG_NAME"
            echo "Expected format: vX.Y.Z (e.g., v1.47.0)"
            exit 1
          fi
          
          # Install dependencies
          echo "Installing dependencies..."
          dart pub get
          
          # Run analysis
          echo "Running dart analyze..."
          dart analyze
          
          # Dry run publish
          echo "Running dry-run publish..."
          dart pub publish --dry-run
          
          # Publish to pub.dev
          echo "Publishing to pub.dev..."
          dart pub publish --force
          
          echo "✅ Successfully published $PACKAGE v$NEW_VERSION"
          
          # Go back to repository root
          cd "$GITHUB_WORKSPACE"
          
          # Create and push package-specific tag after successful publish
          PACKAGE_TAG="dart/${PACKAGE}/v${NEW_VERSION}"
          if ! git rev-parse "$PACKAGE_TAG" >/dev/null 2>&1; then
            git tag -a "$PACKAGE_TAG" -m "Release $PACKAGE v$NEW_VERSION"
            git push origin "$PACKAGE_TAG"
            echo "✅ Created and pushed package-specific tag: $PACKAGE_TAG"
          else
            echo "ℹ️  Tag $PACKAGE_TAG already exists"
          fi

      - uses: rickstaa/action-create-tag@v1
        if: steps.get_tags.outputs.not_enough_tags == 'true' || steps.dart_module_changed.outputs.changed == 'true'
        id: "tag_create"
        with:
          tag: go/${{ matrix.api_module }}/${{github.ref_name}}
          tag_exists_error: false
          message: "release module go/${{ matrix.api_module }}/${{github.ref_name}}"

